import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.holabendez.watchlistranker',
  appName: 'Watchlist Ranker',
  webDir: 'dist'
};

export default config;
